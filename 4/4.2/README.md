- incomplete types can't be instantiated, that's why you can't assign a variable to type `void`

```cpp
int getValue(void) // void here means no parameters
int getValue()     // preferred way in C++; best practice
```

