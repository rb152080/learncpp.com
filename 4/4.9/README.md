```cpp
bool b3 {}; // default initialize to false
```

- by default, when you try to `std::cout` booleans, true is 1 and false is 0
- you can do `std::cout << std::boolalpha;` and then print out `true` and `false properly`