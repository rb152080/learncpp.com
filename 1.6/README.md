quiz
1. uninitialized variable is a variable which you don't assign a value to (through initialization or assignment), it is whatever is stored by default in memory. you should avoid them because it is undefined behavior
2. undefined behavior (is not defined in c++ language) may not be the same each time you run it or based on which machine you use