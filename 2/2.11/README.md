- don't `#include` `.cpp` files

- include header guards
- don't define variables in `.h` files
- only `#include` what you need
- `#include` as little as possible in the `.h` files 
 
![stackoverflow_pic.png](stackoverflow_pic.png)