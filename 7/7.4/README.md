- global variables have global namespace scope/ file scope

- best practice: define global variables in a namespace if they're going to be global

- typical naming convention is to put `g` or `g_` in front of the variable name <- this is also a best practice