- statement in c++: ... ;
- **identifier:** name (functions, variables, classes, structs, namespaces, enums, templates, aliases - typedef, using)
- **preprocessor directive:** `#include <iostream>`

quiz
1. statement is a line that ends with ';' (statement is an instruction)
2. function is a set of statements/instructions (that are ordered sequentially)
3. main function must be in all programs
4. when the program is run, the statements/instructions are run sequentially (in main())
5. c++ statements end in ';' (semicolon)
6. syntax error says that you didn't write the c++ code based on language (violates the grammar rules of the c++ language)
7. c++ stl is the standard template library and it has a bunch of functions (c++ comes with it, precompiled code, its a library for you to use)