- literals and variables both have values
- literals are constant, can't be changed

- the literal is sort of the value (that's the way i see it)

- in a variable, the value has to be fetched from the memory where the variable is stored
- in a literal, you can just use that value itself

- an operator's **arity** is the number of operands an operator can take as input